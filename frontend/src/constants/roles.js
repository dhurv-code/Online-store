/**
 * Role constants
 *
 * Responsibilities:
 * - Define application user roles
 * - Ensure consistent role checks
 */

export const ROLES = {
  buyer: 'buyer',
  seller: 'seller',
  pendingSeller: 'pending_seller',
  admin: 'admin',
  suspended: 'suspended',
}
